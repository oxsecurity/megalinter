descriptor_id: SOLIDITY
descriptor_type: language
descriptor_flavors:
  - all
file_extensions:
  - ".sol"
linters:
  # SOLIDITY LINTER
  - linter_name: slither
    linter_url: https://github.com/crytic
    linter_repo: https://github.com/crytic/slither
    examples:
      - "slither uninitialized.sol"
    install:
      pip:
        - slither-analyzer
