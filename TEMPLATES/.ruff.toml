line-length = 88

[lint]
# Ruff 0.16.0 expanded its default rule set from 59 to 413 rules; pin back to
# the pre-0.16 defaults so MegaLinter's bundled default ruff config keeps its
# prior behavior. See https://astral.sh/blog/ruff-v0.16.0
select = ["E4", "E7", "E9", "F"]
