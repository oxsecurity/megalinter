apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: grafana-secondary
  name: grafana-secondary
spec:
  selector:
    matchLabels:
      app: grafana-secondary
  template:
    metadata:
      labels:
        app: grafana-secondary
    spec:
      securityContext:
        fsGroup: 472
        supplementalGroups:
          - 0
      containers:
        - name: grafana-secondary
          image: grafana/grafana:9.2.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http-grafana
              protocol: TCP
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /robots.txt
              port: 3000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 2
          livenessProbe:
            failureThreshold: 3
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            tcpSocket:
              port: 3000
            timeoutSeconds: 1
          resources:
            requests:
              cpu: 250m
              memory: 750Mi
          volumeMounts:
            - mountPath: /var/lib/grafana
              name: grafana-secondary-pv
      volumes:
        - name: grafana-secondary-pv
          persistentVolumeClaim:
            claimName: grafana-secondary-pvc
